# Container Check adapter

The adapter accepts `dispersal-wolves/container-check/v1` findings. It preserves service names, affected controls, and remediation text as evidence while mapping the tool severity to Pack Sentinel's shared scale.

Recommended interval: fifteen minutes or after every Compose deployment.
