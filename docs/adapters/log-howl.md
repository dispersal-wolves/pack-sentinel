# Log Howl adapter

Log Howl rules can use the generic finding contract. Keep excerpts short and configure upstream redaction for logs that may contain session identifiers or personal data.

Pack Sentinel adds deduplication across repeated rule matches.
