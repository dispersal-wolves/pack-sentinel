# Secret Sweep adapter

Secret Sweep findings become high-severity repository signals unless the source supplies another severity. Raw matches are removed before persistence so the incident database does not become another secret store.

File name, rule identifier, and safe location metadata remain available.
