# Open Ports adapter

New listeners become `network.listener.added` signals. Loopback listeners are low severity; listeners on other addresses are medium severity. Protocol, address, port, process, and state remain attached as evidence.

For useful change detection, invoke the `diff` command against a controlled baseline.
