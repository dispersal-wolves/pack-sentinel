# SSH Guard adapter

The adapter accepts read-only SSH posture findings. Pack Sentinel does not call SSH Guard's apply operation and does not modify daemon configuration.

Run the audit command with the same configuration roots used by the host service.
