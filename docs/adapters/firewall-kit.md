# Firewall Kit adapter

The generic finding contract records validation or drift reported by Firewall Kit. Pack Sentinel never invokes apply operations and cannot change firewall policy.

Point the collector at a read-only inspection command or a wrapper that compares the rendered policy with the active host configuration.
