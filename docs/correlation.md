# Correlation

Rules select recent signals for one host using kinds, minimum severity, and the number of independent sources. Each match produces a stable incident key derived from the rule and host. Rules are deterministic so an operator can reproduce every incident from stored signals.
