# macOS

Run Pack Sentinel with Node.js 24 or newer under a dedicated account when possible. Collector availability varies by operating system, so verify every configured command with `doctor` and a manual collection before scheduling it.
