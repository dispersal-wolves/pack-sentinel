# Windows

Run Pack Sentinel from PowerShell with Node.js 24 or newer. Use absolute executable paths in collector configuration and keep `windowsHide` enabled. A service wrapper may supervise the process; the application itself does not install a Windows service.
